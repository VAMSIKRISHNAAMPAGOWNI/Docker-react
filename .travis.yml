sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t VAMSIKRISHNAAMPAGOWNI/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true VAMSIKRISHNAAMPAGOWNI/docker-react npm run test
